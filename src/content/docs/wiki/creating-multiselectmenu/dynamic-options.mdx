---
title: Dynamic Options
---

Dynamic options update automatically.

For instance, a menu option displaying the current time:

```csharp
menu.AddOption(() => $"Current Time: {DateTime.Now.ToString("HH:mm:ss")}", () => {});
```